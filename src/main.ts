import '@esotericsoftware/spine-pixi-v8';
import { Application } from 'pixi.js';

/**
 * The PixiJS Application instance, shared across the project.
 * Initialized by GameWindow on mount.
 */
export const app = new Application();
